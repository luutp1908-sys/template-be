1. Add auth throttling and abuse controls. (in progress: global throttler + auth route limits implemented)
2. Add caching strategy for hot read endpoints. (in progress: cache foundation + category tree caching implemented)
3. Add load testing with target RPS and p95/p99 latency goals.
4. Add production deployment pattern: horizontal scaling, health probes, rolling updates, and DB pool tuning.
5. Add observability: metrics, traces, and alerts for DB latency, error rate, and auth failures.
